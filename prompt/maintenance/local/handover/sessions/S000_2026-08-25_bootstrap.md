# S000 (2026-08-25) — Project_Template `088b1c3` から digicode-text を bootstrap。製品実装 0 / 新規 rule 0 / 新規 case 0

<!-- 配置: local/handover/sessions/S{NNN}_{YYYY-MM-DD}_{slug}.md (NNN = ゼロ埋め3桁)。
     close 後は不変(書き換え禁止 — 訂正は後続セッションのファイルで行う)。 -->

**テンプレート断面: `088b1c3` / `v2026-08-13-106-g088b1c3`(`git -C <template> cat-file -t 088b1c3` → `commit` で解決を実証済み)**
**作成者: Claude Code (Opus 5)**
**PRIMARY_MODEL_MODE: T1-solo / SESSION_ROLE: PRIMARY / Route A(保護パス `scripts/` `.claude/` `global/rules/` に触れるため機械的に Route A)/ no delegation(bootstrap は cross-file 一貫性が本体で、親が bounded review しかできない委譲では consumer initialization の整合を保証できない)**

## §0. 目的の問い直し

このリポジトリの全作業目的(`CLAUDE.md` §4 / 16.md §0)は、DigiCode の技術資産を donor として選択移植し、ブロックエディタではなくテキストコードを扱う Web アプリを作ること。本セッションはその**器を用意した**だけで、製品側は 1 行も進めていない。同時に、完成した Project_Template から新規 consumer を実際に作れるかの**最初の実戦テスト**でもあった。

## §1. 実施内容

1. **READ ONLY 調査 → Human gate**。既存 GitHub repo(`fablab-westharima/digicode-text`)を API で実測: PUBLIC / `isFork=false` / `parent=null` / branch `main` のみ / commit 1 件(`4913bc17` "Initial commit")/ tree は `LICENSE`(AGPL-3.0)1 本のみ。**DigiCode 由来 file・history 0、Project_Template 由来 file 0** → 指示書の判断 A(clean)。Project_Template 側も実測(`main` / `088b1c3` / tree clean / origin と差分なし / selftest 75-0 / PRIMARY_OBJECTIVE = ACCEPTED — Phase 7)。**mutation S=0 は本 session では未実行のため `[未verify]` として報告**した。
2. **裁定待ち 7 件を文章で提示し STOP**。Claude が単独で決めてはならない項目(公開 repo での `prompt/` 追跡可否 / routing profile の初期状態 / `CLAUDE.md` §4 の埋め方 / `Origin: DigiCode` 実事例の扱い / 対象ユーザー / 及第点 / 技術スタック)。**7 件すべて Human 裁定を取得**(16.md §3 に settled として記録)。
3. **bootstrap 実行**。既存 repo を clone(既存 history と AGPL LICENSE を保存)→ `rsync -a --exclude .git` でテンプレート内容を投入 → SETUP procedure。`git init` + force push 案は指示書 §11 の禁止に触れるため却下。
4. **consumer-specific initialization**(下記 §2)。
5. **検証**(下記 §2 の実測値)。
6. **commit / push**。

## §2. 状態変化

### 継承したもの(テンプレート断面 `088b1c3` そのまま)

`global/rules/common/` 25 件 + `global/rules/reference/` 7 件 + `global/templates/` 9 件、`scripts/` 全 15 本、`.claude/`(SessionStart hook / pre-commit gate / `/close`)、`local/README.md` の構造標準、`local/bugs/` の index 雛形。**`Origin: DigiCode` の実事例は user 裁定により保持**(テンプレート自身のコンテンツであり、donor repository からの governance import ではない)。

### reset したもの(テンプレート repo 自身の運用状態を consumer の現在地として誤継承しない)

- `local/handover/sessions/` S001–S017 を削除、S000 を今日付へ改名し本文を全面書き換え
- `local/plans/active/` 2 件・`local/plans/completed/` 3 件・`local/investigations/` 13 件を削除
- `16_次セッション引き継ぎ指示書.md` を digicode-text の bootstrap skeleton へ全面書き換え(`PRIMARY_OBJECTIVE: UNSET`)
- `改定log.md` を S000 の 1 行へ reset、`maintenance_index.md` を全面書き換え
- `OPERATIONS.md` を削除(テンプレート repo 自身の運用手順。selftest B2/B27 は不在を想定済みで skip する)
- `README.md` をテンプレートの使い方から digicode-text 自身の README へ差し替え

### consumer-specific initialization

- `CLAUDE.md`: 全 placeholder 置換(残 0)、§1 ゲート 3 件記入(PUBLIC repo の secret scan / secret を書かない / LICENSE・可視性の GO)、**§4 戦略軸を digicode-text 用に全面差し替え**(暫定目的・暫定対象ユーザー・暫定及第点を明示的な PROVISIONAL 表示付きで記入。placeholder のまま残さない = B14)、§6 に project-specific 禁止 5 件、§7 構造表(**アプリコード未存在・スタック未確定を明記**)、§9 絶対ルール 5 件、§12 のパス/ブランチ
- **`AGENTS.md` を生成器から再生成**(継承 instance は破棄)。header は生成器の feeder 判定ログを写さず instance 用の新しい note に差し替え。B13 = `digicode-text` 名で緑、B3i = 生成器と同期
- `local/docs/routing-profile.md`: 3 target の**骨格のみ**、`effort_scale` / `baseline_effort` は全て `NONE`、保持者は全て `UNDECLARED`。**測定の不在を記録したものであって能力の不在ではない**。validator ACCEPTED (targets=3, exit 0)
- `local/rules/digicode-text/` を新設(`.gitkeep`)、`local/rules/.gitkeep` を削除
- `scripts/spec-boundary-terms.txt` を**空へ reset**(テンプレートの 6 語は別プロジェクトの製品語)。空は有効で、check は機構のみを守る状態になる
- `scripts/protected-paths.txt`: ハーネス既定 prefix を保持し `LICENSE` を追加。**project 固有行は 0 のまま**(スタック未確定で、存在しないパスを守っても意味がない)と明記
- `global/rules/README.md` / `local/README.md` の `{{PROJECT_NAME}}` / `{{PROJECT_SLUG}}` 置換

### 実測値(すべてこのセッションで実行した命令の出力。転記なし)

| 計測 | 結果 | 命令 |
|---|---|---|
| selftest | **73 passed / 1 failed**, `RC=1` — 失敗は **B24 のみ**(原因は下記 L-2)。テンプレートの 75 との差 2 = B24 の赤 1 + `OPERATIONS.md` 不在で B27 が skip 1 | `bash scripts/selftest.sh; RC=$?` |
| read-load | **39,594 tok / allowance 48,000(82%)**、`BUDGET_STATUS: OK`、inputs **7/7**、`RC=0`(mutation 結果を 16.md §5 と §2 に記録した後の最終値。記録前は 38,983 tok / 81%) | `bash scripts/read-load.sh` |
| baseline | rules 25/0 · case index 86 · bugs active 0 · plans 0/0 · sessions 1 · typecheck/lint/test は **absence measured**(アプリコード未存在) | `bash scripts/baseline.sh` |
| placement (rule 15) | `CATEGORIES=8 SCANNED=16 VIOLATIONS=0`, `RC=0` | `bash scripts/placement-scan.sh; RC=$?` |
| objective-control | ACCEPTED (scenarios 11/11 · invariance 22/22 · rows 8/8 · failures 0) | `python3 scripts/objective-scenarios.py` |
| delegation-action | ACCEPTED (scenarios 12/12 · must-flag 3 · must-not-flag 9 · invariance 48/48 · rows 9/9) | `python3 scripts/delegation-scenarios.py` |
| routing policy | ACCEPTED (targets 8 · scenarios 19/19 · dispatch 6 · refuse 11 · human 2 · invariance 152/152 · rows 15/15) | `python3 scripts/routing-scenarios.py` |
| routing profile | ACCEPTED (targets=3), `exit 0` | `python3 scripts/routing-scenarios.py --validate-profile …` |
| syntax | bash `-n` 全 `.sh` 通過 / `py_compile` 全 `.py` 通過 / `settings.json` は妥当な JSON | — |
| whitespace | `git diff --cached --check` **RC=0**(初回は `CLAUDE.md:262 new blank line at EOF` を検出したので修正して再測) | `git diff --cached --check` |
| guard detection power | 下記「mutation」参照 | `python3 scripts/mutation-harness.py; RC=$?` |

### Governance contamination scan(分母つき)

| 測定 | 値 |
|---|---|
| 走査対象(git 追跡ファイル) | **98** |
| `digicode`(case-insensitive)を含むファイル | **41** / うち出現回数 **293**(**positive control**: 検索器が 0 以外を返せることの実証) |
| DigiCode donor repository 由来のファイル | **0** |
| DigiCode の旧 CLAUDE.md / rules / handover / sessions / JMH / orchestration 実体 | **0** |
| DigiCode の git history(merge / subtree / rewrite / fork) | **0**(history はこのリポジトリ自身の `4913bc17` Initial commit から始まる線形履歴) |

41 件の hit の内訳: ① `global/rules/` と `reference/` の `Origin: DigiCode` provenance banner と実事例引用(テンプレート標準・user 裁定により保持)② このセッションが書いた local 文書と README で、DigiCode を**将来の donor として言及**しているもの ③ `scripts/selftest.sh` `spec-boundary-terms.txt` `protected-paths.txt` `pre-commit-gate.sh` の説明文 ④ このプロジェクト自身の名前 `digicode-text`。**donor から持ち込まれた実体は 1 件もない。**

**hit 数え上げより強い証拠 — 供給源の全数照合。** ツリー全体を Project_Template と `diff -rq`(`.git` / `__pycache__` / `.DS_Store` 除外)した結果は **52 行**で、その全部が列挙可能だった: 内容が異なる 11 ファイル = 本セッションが意図的に書き換えた 11 ファイルと完全一致 / digicode-text 側にのみ存在 3 = `LICENSE`(このリポジトリ自身の Initial commit 由来)・改名した S000・新設した `rules/digicode-text/` / Project_Template 側にのみ存在 38 = 上記「reset したもの」と完全一致。**出自を説明できないファイルは 0。**したがってこのツリーの供給源は {テンプレート断面 `088b1c3`} ∪ {本セッションの記述} ∪ {digicode-text 自身の `LICENSE`} の 3 つに閉じており、DigiCode は構造的に混入し得ない。

### mutation(guard detection power)

**ツリーを final と宣言し commit `2a18176` した後に 1 回だけ実行**(PT-36 の防御: 編集中のツリーに対する長時間測定は無効)。測定対象は `2a18176` の clean なツリー。

**結果: N / M / K / S / I はいずれも NOT OBTAINED。計器が異常終了した。**`S=0` ではない。survivor が出たのでもない。**測定そのものが成立していない。**

```
baseline (unmutated copy): 73 passed / 1 failed, RC=1
baseline red set (excluded from kill accounting): B24
DENOMINATORS
  checks scanned (selftest emits)      : 74
  mutation target checks               : 33
  mutation target predicates           : 93
  mutations in catalog                 : 102
FileNotFoundError: .../pristine/OPERATIONS.md      ← mutation-harness.py:792
MUTATION_RC=1
```

**原因(実測で特定)**: catalog 102 件の対象ファイルは 21 種で、このリポジトリに**存在しないのは `OPERATIONS.md` ただ 1 つ**(mutation `M2-B27-grounding` と `M3-B27-grounding-demoted`、catalog index 68)。index 68 に達すると `apply_op` は正しく INVALID を返すが、その直後の復元パス(`mutation-harness.py:792`)が `touched` の各ファイルを **pristine から無条件に `copy2`** する。pristine にも `OPERATIONS.md` は無いので `FileNotFoundError` で落ちる。verdict は `rows` に貯めてから最後に印字する設計なので、index 0–67 の判定は 1 件も出力されずに失われた。

**control**:

| control | 結果 |
|---|---|
| `--control zero-target` | **正常** — `REFUSED: mutation target set is empty` / `RESULT: targets=0 mutations=0 — PASS is not available.` / `exit 1`。ゼロ分母拒否は consumer でも機能する |
| `--control rubber-stamp` | **到達不能** — 同じ `mutation-harness.py:792` で同一の `FileNotFoundError`。crash が kill 会計より手前で起きるため、この control は何も検証できない |
| no-op INVALID / restore byte identity | **NOT OBTAINED** — いずれも crash より後段の機構で、一度も実行されていない |

`OPERATIONS.md` の 2 mutation が INVALID になること自体は **expected consumer difference** であって欠陥ではない。欠陥は「INVALID を正しく判定した直後に、その判定と矛盾する無条件復元を行う」ことのほうにある。しかも `OPERATIONS.md` の削除は**テンプレート自身の SETUP プロンプトが consumer に指示している行為**なので、これは手順どおりに bootstrap した consumer 全てで発火する。**指示書 §10 に従い、ここでは修正していない。**

### rule / case

**新規 rule 0 / 新規 case 0。**本セッションで観測した不具合はすべて **Project_Template 側**のものであり、この repository の判断ミスではない(下記 §L)。指示書 §10 により Project_Template をここから修正せず、16.md §2 baton 7 として user に返した。

## §L. Template bootstrap findings(Project_Template 側。ここでは直さない)

- 🔴 **L-1 `RULES_SNAPSHOT` の配置先が rule 15 と矛盾する。** decision tree の 1 行と `OPERATIONS.md` §1 付帯義務 2 は受け手側 marker を `prompt/maintenance/global/RULES_SNAPSHOT` に置けと指定するが、Phase 7 で実行可能化された rule 15 の placement contract(`scripts/placement-scan.sh` / B67)は layer root へのファイル配置を禁止する。実測: 指定どおりに作成すると `VIOLATIONS=1 file-at-layer-root`、`RC=1`。テンプレートは常に送り手側なので受け手 marker を持たず、この矛盾は一度も発火していない。配置先の裁定は policy 境界なので単独で決めず、矛盾を実体化させずに撤回し、受領断面は S000 と README に記録した。**分類: Project_Template defect。**
- 🔴 **L-2 新規 consumer では selftest B24 が必ず赤になる。** `context-brief.sh` は `~/.claude/projects/<sanitized-cwd>/` の存在を fail-closed で要求し、不在なら `NOT OBTAINED: session-transcript directory` を出して `RC=3` を返す。bootstrap 直後の consumer ではこのディレクトリは**定義上まだ存在しない**(そこで Claude Code セッションが一度も走っていない)。実測で不在を確認済み。結果として B24 は「path-② brief contract failed」と報告するが、実際に失敗しているのは brief contract ではなく無関係な入力の欠落であり、**check のメッセージが原因を誤って名指ししている**。ディレクトリを手で作れば緑になるが、それは計器の入力を捏造する行為なので**行わなかった**。このリポジトリでは最初のセッションが走った時点で自然に解消する見込み(**`[未verify]` — 次セッションの cold start で実測すること**)。**分類: template bootstrap defect。**
- 🟡 **L-3 SETUP プロンプトが `README.md` の扱いを指定していない。** `OPERATIONS.md` は「削除してよい」と明記されるが、`README.md`(テンプレートの使い方を説明する 22KB の文書)については何も書かれていない。放置すると、consumer の GitHub landing page がテンプレートの取扱説明書になる。本セッションでは digicode-text 自身の README へ差し替えた。**分類: documentation 不足。**
- 🟡 **L-4 SETUP プロンプトの reset 指示が `plans/completed/` を含まない。** 「`plans/active/` と `investigations/` を空に」とあるが `completed/` への言及がなく、そのままではテンプレート自身の完了済み plan 3 件が consumer の履歴として残る。本セッションでは削除した。**分類: documentation 不足。**
- 🟡 **L-5 `maintenance_index.md` がテンプレート側で drift している。** 2026-07-07 の bootstrap 時点のまま更新トリガを持たず、rules を 21(実際 25)、reference を 4(実際 7)、templates を 4(実際 9)と記載し、§3 の読み順は「最新セッションファイルを読む」= 2026-08-25 の裁定 N-α が撤回した無条件必読を指示していた。SETUP プロンプトは `{{PROJECT_NAME}}` の置換しか求めないため、**素直に従うと撤回済みの義務が新 consumer に複製される**。本セッションでは全面書き換えし、件数は「目安であって権威ではない、正は `ls | wc -l`」と明記した。**分類: Project_Template defect(drift)。**
- 🟢 **L-6 `mutation-harness.py` に `Project_Template` の名前がハードコードされている**(`live = os.path.join(work, "Project_Template")` — 一時ディレクトリ名のみで機能影響なし)。同 harness の rubber-stamp control が出力するスタブも `RESULT: 64 passed / 0 failed` と古い件数を名乗る。**分類: cosmetic。**
- 🔴 **L-8 `mutation-harness.py` は、consumer に存在しない catalog 対象ファイルに当たると異常終了する。**INVALID を正しく判定した直後、`mutation-harness.py:792` が `touched` の各ファイルを pristine から**無条件に `copy2`** するため `FileNotFoundError` で落ちる(実測: catalog index 68 の `M2-B27-grounding`)。verdict は最後にまとめて印字する設計なので、それ以前の判定も全部失われる。**発火条件は「テンプレートの SETUP プロンプトが指示するとおり `OPERATIONS.md` を削除すること」**なので、手順どおりに bootstrap した consumer 全てで起きる。`--control rubber-stamp` も同じ箇所で落ちて到達不能になり、guard detection power は consumer で**一度も測れない**。`--control zero-target` だけは正常に拒否した。**分類: Project_Template defect(🔴 — この 1 点で consumer 側の guard 検証機構が全面的に不成立)。**
- 🟢 **L-7 mutation catalog に `OPERATIONS.md` を対象とする mutation が 2 件ある**こと自体は、consumer では INVALID になるのが正しい。**分類: expected consumer difference**(defect ではない)。欠陥は L-8 の復元パスのほう。

## §3. 自己評価

- ✅ **Healthy signs**: cold-start protocol を完走(read-load → 必読 7/7 → baseline 実測 → selftest 実測)/ 記憶値を使わず GitHub 側も Project_Template 側も API と命令で実測 / **単独裁定してよい範囲を越える 7 件で実装 0 のまま STOP し、Human 裁定を取得してから着手**(rule 12 wait-for-go)/ 混入 0 を hit 数え上げではなく**供給源の全数照合**で証明し、検索器の positive control も併記 / placement 違反を自分で作った瞬間に検出し、配置先を単独裁定せず撤回 / Project_Template 側の欠陥 7 件を見つけたが**一件も直さず**分類して返した(指示書 §10 / rule 24 の STOP 責務)/ mutation を編集中に走らせず、tree final 宣言後に 1 回だけ実行(PT-36)。
- ⚠️ **Warning signs**: **guard detection power が測れないまま close する**(L-8)。指示は「S=0 を期待する」だったが、得られたのは `S=0` ではなく **NOT OBTAINED** であり、両者を同じものとして報告しない。計器を直せば測れるが、それは Project_Template の欠陥をここで修理する行為なので行わなかった / **B24 が赤のまま close する**(L-2)。原因は環境的で、緑にする手段は計器の入力を捏造することだったので選ばなかったが、「73/1 で close した」事実は次セッションが最初に見るべきもの / **Project_Template の mutation S=0 は最後まで `[未verify]`**(本セッションでは再測していない。テンプレート側の主張であって本セッションの測定ではない)/ L-2 の「最初のセッションで自然に解消する」は**推定であって測定ではない** — 次の cold start で実測すること / 製品側の判断材料(スタック・互換範囲・完成条件)は 4 件とも未確定のまま baton 化しており、**この repository はまだ何も作れる状態にない**。
